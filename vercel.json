{
  "buildCommand": "cd apex-auto-glass-erp && npm install && npm run build",
  "outputDirectory": "apex-auto-glass-erp/dist",
  "devCommand": "cd apex-auto-glass-erp && npm run dev",
  "installCommand": "cd apex-auto-glass-erp && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

