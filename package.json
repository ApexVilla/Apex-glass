{
  "name": "apex-glass-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd apex-auto-glass-erp && npm install && npm run build",
    "install": "cd apex-auto-glass-erp && npm install",
    "dev": "cd apex-auto-glass-erp && npm run dev"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "node-forge": "^1.3.3",
    "soap": "^1.6.1",
    "xml-c14n": "^0.0.6",
    "xml-crypto": "^6.1.2"
  }
}
