{
  "name": "apex-glass-root",
  "version": "1.0.0",
  "private": true,
  "description": "Apex Glass ERP - Root package for monorepo structure",
  "scripts": {
    "build": "cd apex-auto-glass-erp && npm install && npm run build",
    "dev": "cd apex-auto-glass-erp && npm run dev",
    "install:sub": "cd apex-auto-glass-erp && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
