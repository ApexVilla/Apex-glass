# 🔧 Instalar Comando `apex-glass`

## ✅ Instalação Automática (Já foi feita!)

O comando `apex-glass` já foi instalado automaticamente.

## 🚀 Como Usar

Agora você pode simplesmente digitar no terminal:

```bash
apex-glass
```

O comando vai:
1. Verificar se o servidor está rodando
2. Se não estiver, perguntar se você quer iniciar
3. Oferecer as opções de abrir pelo IP ou Localhost
4. Abrir automaticamente no navegador

## 📝 O que foi feito:

1. ✅ Script criado: `apex-auto-glass-erp/apex-glass`
2. ✅ Link simbólico criado: `~/bin/apex-glass`
3. ✅ Diretório `~/bin` adicionado ao PATH no `.bashrc`

## 🔄 Se o comando não funcionar

Se ao digitar `apex-glass` você receber "comando não encontrado", execute:

```bash
# Recarregar o shell
source ~/.bashrc

# Ou abrir um novo terminal
```

## 🗑️ Desinstalar

Se quiser remover o comando:

```bash
rm ~/bin/apex-glass
```

